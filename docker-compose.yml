version: '3.8'

services:
  agent-studio:
    build: .
    container_name: react-agent-studio
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      # Mount workspace for safe read/write tool testing, permitting hot reload inside Docker
      - .:/app
    restart: unless-stopped
